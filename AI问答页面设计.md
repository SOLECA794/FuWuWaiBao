我完全明白你的需求，你要的是**一套精准、无歧义、AI绝对能看懂、不会跑偏的指令prompt**，能让AI精准复刻NextChat的对话交互效果，生成能直接嵌入你学生端的代码，同时给你拆解清楚核心复刻逻辑，方便你随时修正。

---

## 一、直接复制给AI的【精准指令prompt】
你直接把下面这段完整内容喂给AI，它绝对能明白你要的效果，不会干不明白：
```
# 开发需求：复刻NextChat的AI对话交互组件，嵌入Vue3学生端项目
## 项目背景
我正在开发一个高校AI教学平台的学生端，需要在我的项目里嵌入一个AI问答模块，**1:1复刻NextChat（ChatGPT-Next-Web）的核心对话交互效果与UI细节**，不用实现后端接口逻辑，前端模拟所有交互效果即可。

## 技术栈约束

2.  适配我现有项目的浅色主题风格，，卡片式布局，圆角12px，和NextChat的布局逻辑一致，但适配浅色主题
3.  所有后端接口逻辑用前端模拟实现，不用真实请求，预制AI回复内容即可
4.  对话历史记录必须存在localStorage里，页面刷新不丢失
5.  代码必须可直接复制到我的项目里运行，无额外依赖


### 1. 布局结构参考：
- 整体采用「左侧窄栏（会话列表）+ 右侧宽栏（对话主区）」的左右分栏布局
- 左侧会话列表：
  - 顶部「新建对话」按钮，点击创建新的空白对话
  - 会话列表项展示：对话标题、最新消息预览、最后对话时间
  - 选中的会话高亮显示，hover有上浮/背景变色效果
  - 每个会话项支持右键/点击删除对话
- 右侧对话主区：
  - 顶部：对话标题、清空对话按钮、重新生成按钮
  - 中间：对话消息滚动区，自动跟随最新消息滚动到底部
  - 底部：固定输入框区域，包含输入框、发送按钮

### 2. 对话气泡与消息展示（核心视觉效果）（颜色请自行搭配，适配整体项目主体）
- 用户消息：靠右对齐，深蓝色背景（#2d8cf0）、白色文字、圆角气泡，左侧带用户头像占位
- AI消息：靠左对齐，浅灰色背景（#f1f3f6）、黑色文字、圆角气泡，左侧带AI头像占位
- 每条AI消息右下角必须带「复制」按钮，点击一键复制消息内容，有成功提示
- 消息里的代码块必须带语法高亮、复制按钮，和NextChat效果一致
- 消息必须展示发送时间，格式为YYYY-MM-DD HH:mm

### 3. 核心交互效果（必须和NextChat完全一致）
1.  流式打字机效果：AI回复必须模拟SSE流式输出，一个字一个字逐行显示，不是一次性全部渲染，和真实AI对话效果一致
2.  输入框交互：
    - Enter键发送消息，Shift+Enter键换行，和NextChat完全一致
    - 发送后自动清空输入框，对话区自动滚动到底部
    - 输入框为空时，发送按钮禁用
    - AI回复过程中，发送按钮禁用，防止重复发送
3.  加载状态：AI回复过程中，显示「正在思考...」的加载动画，和NextChat的加载效果一致
4.  对话切换：点击左侧会话列表，右侧自动切换对应对话的消息记录
5.  新建对话：点击新建对话，自动创建空白对话，切换到新对话页面

### 4. 前端模拟逻辑（不用后端）
- 不用对接真实LLM接口，用户发送任意消息后，前端用setTimeout模拟流式输出预制的AI回复内容即可
- 所有对话数据、会话列表都存在localStorage里，刷新页面不丢失
- 预制3条以上不同的AI回复内容，随机返回，模拟真实对话效果

## 可选嵌入功能（按优先级实现，不强制）
1.  消息撤回功能：用户发送消息后1分钟内可撤回
2.  对话内容一键导出为图片/文本功能
3.  预设提示词功能：输入框上方显示常用的学习类提示词（比如「解释这个知识点」「给我出配套习题」「总结这个章节」），点击自动填入输入框
4.  深色/浅色主题切换功能，适配我现有项目的主题
5.  消息引用功能，可引用之前的对话内容提问

## 输出要求

2.  代码必须有清晰的注释，关键交互逻辑必须标注说明

4.  不要给我无关的内容，所有代码必须可直接运行，无报错
```

---

## 二、核心复刻逻辑拆解（你自己看，方便修正AI的输出）
我给你的prompt里，把NextChat的核心效果拆成了**不可拆分的4个核心模块**，AI绝对不会跑偏：
1.  **布局结构**：先定死「左会话列表+右对话区」的框架，和你学生端的左侧导航完全适配，不会和你现有页面冲突
2.  **视觉细节**：明确气泡的对齐、颜色、头像、代码高亮、复制按钮，完全复刻NextChat的专业感，不会出现简陋的样式
3.  **交互灵魂**：把你最在意的「流式打字效果、Enter发送/Shift+Enter换行、加载状态、自动滚动」这些核心交互写死成必做项，AI不会漏掉
4.  **落地适配**：明确要求Vue3+ElementPlus、前端模拟、localStorage存数据，完全适配你现有的项目，不用改后端，拿来就能用

---

## 三、如果AI给的效果不对，你可以用这些精准指令修正
| 问题场景 | 精准修正指令 |
| :--- | :--- |
| AI给的流式效果不对，不是逐字输出 | 重新优化AI回复的流式打字机效果，必须和NextChat完全一致：一个字一个字逐行渲染，不是分段输出，用前端递归setTimeout实现，模拟真实SSE流的效果，打字速度均匀，不会卡顿 |
| 输入框的Enter/Shift+Enter逻辑不对 | 修正输入框的键盘事件：按下Enter键直接发送消息，按下Shift+Enter键只换行不发送，和NextChat的交互逻辑完全一致，发送后自动清空输入框，光标聚焦回输入框 |
| 样式和我现有项目不匹配 | 把所有样式改成浅色主题，主色调统一为#2d8cf0，卡片圆角统一为12px，和我现有学生端页面的UI风格完全统一，去掉深色主题的样式，只保留浅色主题 |
| 对话记录刷新就丢了 | 修正数据存储逻辑，所有会话列表、对话消息必须完整存在localStorage里，页面刷新后自动读取localStorage里的数据，完全恢复之前的对话内容，不会丢失 |
| 对话区不会自动滚动到底部 | 优化对话滚动逻辑：用户发送消息、AI流式输出的每一步，都必须自动把对话滚动区滚动到最底部，和NextChat的效果完全一致，不会出现新消息被挡住的情况 |