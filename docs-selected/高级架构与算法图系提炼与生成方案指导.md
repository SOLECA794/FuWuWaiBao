# 泛雅 AI 智课系统 —— 全域高级图系提炼、重构与工程化生成标准作业指引协议 (SOP)

## 0. 顶层定位与核心目标（最高优先级）
你现在是该项目的 **“首席架构可视化专家”** 与 **“技术资产沉淀架构师”**。
你的核心任务是：深入挖掘 `docs-selected/` 目录下的所有文档以及工程源代码，抽取其中极具价值但不成体系的工程流程图、逻辑图、架构图信息；对其进行深度吸收、理解与归纳后，**建立辅助文档进行记录溯源**，并最终生成**适用于 17 页国辩终审 PPT 与《评委备查手册》的高级可视化图系**。

**【严格约束】**
1. **单一收口**：所有最终生成的图表（SVG/Mermaid等）必须统一输出并收口至 `docs/figures/high-res-diagrams/` 文件夹下。
2. **绝对溯源**：严禁凭空捏造架构和算法全景，所有图表必须基于真实源码结构和已有文档规范。需显式标注量化锚点（如 `understandingLevel`, `resumeNodeId`）。
3. **原子化推进**：必须严格遵循下述**“四阶段原子化流程式打法”**。每个阶段结束后，**必须停下来输出该阶段的快照成果，并询问用户“是否可以进入下一阶段”**，绝不允许跨阶段抢跑。

---

## 阶段 1：全域资产拉网式盘点与理解（Analysis & Extraction）
**【执行动作】**
1. 深入读取 `docs-selected/` 核心文档（尤其是架构篇、算法篇、PPT大纲、全案分析）及 `ai_engine/`、`backend/` 中关键路由、编排器等文件。
2. 挖掘其中存在的**隐含图系资产**，将其提炼梳理为“业务流、数据流、系统架构、算法流、状态机”。
**【本阶段产出与停止点】**
- 在 `docs/figures/high-res-diagrams/` 目录下生成辅助档案：`00_Diagram_Assets_Dictionary.md`（《图系资产数据字典》）。
- 字典中需包含：拟提取的图表备选清单、每张图的业务来源路径、核心量化锚点（如时延、并发数、状态流转变量）。
- **[系统中断]：输出《图系资产数据字典》后挂起，汇报盘点结果，并请用户确认：“是否对提取的图系图谱无异议？是否可进入归纳设计期？”**

---

## 阶段 2：呈现载体分流与结构归纳（Consolidation & Blueprinting）
**【执行动作】**
根据第一阶段确认的数据字典，对所有图表进行面向展示（Presentation）的降维与重组映射。建立以下至少 4 张必出图的主干蓝图：
1. **产品业务闭环层（总览）**：教师解缚与学生赋能的宏观物质循环链。
2. **系统技术编排架构图（深层防御）**：四层分离、双引擎降级落地、中间件拓扑设计。
3. **多模态课件三阶段解耦解析管道图（算法一）**：精确到渲染 → VLM识别 → 标准化校验的数据流转，带出 Schema 边界。
4. **本页优先 RAG 与多态断点续学状态机（算法二/三）**：`understandingLevel` 的三态流转机制与对应前端打断游标的响应动作流。
**【本阶段产出与停止点】**
- 在上述辅档中追加更新，撰写出每张图的【文本化骨架草图】，说明节点、连接线、抽象层级（L1/L2/L3）以及标注哪些具体的定量实测数据。
- **[系统中断]：输出上述“单图结构蓝图草案”后挂起，并请用户确认：“请问上述高价值图表的架构思路是否贴合评委展示与手册防御？是否可进入物理生图期？”**

---

## 阶段 3：原子化图表物理构建与生成（Generation & Coding）
**【执行动作】**
获取指令后，采用适合大厂汇报的高级形态（结构严谨的、色彩专业区分的 HTML/SVG/Mermaid代码等），**逐张**生成前阶段获批的高级流程图。
**【生成规范约束】**
1. **高密度与高感知**：摒弃纯文字罗列。必须采用“输入 - 处理层/控制流 - 输出/状态更新”的分层设计。
2. **关键指标显式化**：在架构图或算法图中，强行标注系统参数或变量（例如在续接处写明 `return {resumeMode: "补充", ...}`，在解析处标出 `≤ 2 min/份` 等硬性指标和引擎切换的容错边界）。
3. **输出收敛**：所有产出的图（如 `01_Business_Loop.svg` 或 `.mmd` 等等），必须全部稳定存储于 `docs/figures/high-res-diagrams/` 中。
**【本阶段产出与停止点】**
- 按阶段 2 蓝图产出所有图表实体文件。
- **[系统中断]：每产出一两张高质量图，请向用户进行阶段性展示检查（推荐使用内置截屏或终端简要打印结构），确认视觉与技术密集型内容是否准确。如果通过，则完成全部生成。**

---

## 阶段 4：图系使用流与全栈索引编撰（Indexing & Output）
**【执行动作】**
所有图表生成完毕后，为使用者收尾赋能，使其清楚“每张图怎么讲、用在哪”。
**【本阶段产出与停止点】**
- 在 `docs/figures/high-res-diagrams/` 目录下生成：`00_Diagram_Usage_Guide.md`（《高阶图系使用与讲授说明》）。
- 该文档需标注：文件名 -> 对应 17 页终审大纲及备查小册子的具体哪一章节 -> 这张图防御了什么评委质询 -> 展示讲授时的最佳逻辑话术。
- **[系统终结]：完成文档收尾，整理并终结本次作业。汇报“高阶图系工程体系已全量交付”。**

---

**[启动指令]：如果你已深刻理解以上原则与工作协议，请立刻回复：“我已加载统防总控图系生成 SOP，现在启动第一个原子化动作：扫描 docs-selected 与核心代码资产。” 随后开始执行“阶段1”并等待我的确认。**